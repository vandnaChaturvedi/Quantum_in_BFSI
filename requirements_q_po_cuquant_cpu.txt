# Requirements for q_po_cuquant_cpu.ipynb
# Quantum Portfolio Optimization — cuStateVec CPU Simulator
#Python version : 3.10.x
# Tested on      : Linux x86_64 (Ubuntu 20.04 / 22.04)
# Jupyter kernel : conda base environment
#
# ── cuStateVec CPU Notes ─────────────────────────────────────
# cuStateVec CPU is BUNDLED inside qiskit-aer 0.17.0.
# No separate cuquantum or custatevec package install needed.
# Activated in code via:
#   AerSimulator(method='statevector', device='CPU', cuStateVec_enable=True)
#
# cuStateVec CPU version : bundled with qiskit-aer 0.17.0
#                          (corresponds to cuQuantum 24.03 CPU-only build)
# Available since        : qiskit-aer 0.12.0
#
# To upgrade to GPU (requires CUDA 12 + NVIDIA GPU):
#   pip install qiskit-aer-gpu==0.17.0   # replaces qiskit-aer
#   pip install cuquantum-python          # NVIDIA cuQuantum Python bindings

qiskit==1.4.2
qiskit-aer==0.17.0
qiskit-algorithms==0.3.1
numpy==1.26.4
scipy==1.15.2
pandas==2.3.3
matplotlib==3.10.6
ipykernel==6.31.0
ipython==8.30.0
jupyter==1.1.1
jupyterlab==4.4.7
